# ParseAtlas research recovery — 2026-09-07

This is archival recovery, not a new optimization or performance measurement.

## Provenance

The research was absent from the working tree and current stash list. Git's
unreachable objects retained the stash:

- Stash: `895f8d96a67220e2ae8fd89c7ceb38479544298b`.
- Recorded subject: `On experiment/representation-search: experiment/representation-search: field reduce-carry WIP (auto-stashed before dev CI repair)`.
- Recorded time: `2026-09-06T14:25:07+00:00`.
- Base: `fef231d4e4173bd016fb2a3a1eff67087396a203`.
- Untracked-files parent: `8d3658818212703958e2573ecccfcdfd5d1eda00`.
- Research subtree: `834e7712545402e9535739f5834653711b69fd30`.

All 269 files in that research subtree were restored byte-for-byte to their
original paths. No destination file existed before recovery. Existing cache
directories were preserved. The stash's four tracked changes were also restored:
the ParseAtlas clone ignore rule, `field.cpp`, `field_asm_x64_gas.S`, and
`audit/test_regression_field_reduce_carry.cpp`. The three arithmetic/test hashes
match the corrected P1 reference, not the unmodified base commit.

The local `research/ParseAtlas` clone and AIWorkHub runtime are not included in
this archival commit. No stash was popped, dropped, or cleared during recovery.
The evidence establishes a previously created stash; it does not establish who
removed its reference or whether that happened through pop, drop, or another action.

## Recovery validation

- 269 restored files match their original Git blob identities.
- P2–P7 manifests: 253 SHA-256 checks, zero errors (41, 42, 52, 50, 50, 18).
  Historical frontier references use their original per-phase snapshots.
- Preserved P7 executable, CUDA object and oracle, and corrected P1 static library
  match their previously recorded SHA-256 identities.
- Preserved carry-regression executable: 45/45 checks pass.
- Preserved P1 oracle: 755,199 field and 716,741 scalar assertions, zero mismatches.
- Preserved P7 oracle: 78,521 assertions, zero mismatches; checksum
  `faa743e95a5c964d`.

These executions replay preserved binaries; they are not fresh compilation,
new timing series, constant-time certification, or GPU sanitizer qualification.
Original reports retain their original platform and qualification limitations.

## Byte-preservation exceptions

The default staged `git diff --check` reports existing whitespace in five
recovered files: one blank line at EOF each in `F1_REDUCTION_PROTOCOL.md`,
`MODN_NATIVE_PROTOCOL.md`, and `probes/p6_field_boundary_compare.cpp`; 402 trailing
whitespace sites in `data/f1_disassembly_20260905.txt`; and 11 in
`data/p1_asm_disassembly_20260905.txt`. These original bytes are intentionally
retained for evidence identity. This is a recorded archival exception, not a
claim that the default staged whitespace gate passes.

## Continuation

P7 is the last completed experimental phase. The two P8 property-relation
research cards have no delivered research result; their last recorded launch
failure required refreshing the Claude subscription session. Recovery does not
mark either card accepted. Refresh Source Graph and revalidate task inputs and
provider readiness before resuming P8. The owner's interpretation remains:
one object, many visible properties, their relationships, then jointly enabled
constructions and falsifiable native C++ experiments.
